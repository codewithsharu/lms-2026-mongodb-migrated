import Layout from '../../components/Layout';
import CompilerChallengeBrowser from '../compiler/ChallengeBrowser';

const TeacherChallengeBrowser = () => {
  return (
    <Layout>
      <CompilerChallengeBrowser />
    </Layout>
  );
};

export default TeacherChallengeBrowser;
